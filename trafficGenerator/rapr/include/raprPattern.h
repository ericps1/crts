#ifndef _RAPR_PATTERN
#define _RAPR_PATTERN

/*!
Helper class to build tables to map strings to values
*/
/* return if we don't keep mgenPattern.h
class StringMapper
{
    public:
        const char* string;
        int         key;
};  // end class RaprStringMapper
*/
#endif // _RAPR_PATTERN
