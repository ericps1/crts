// This is a template which can be used as a basis to create other master scenario 
// configuration files. It is used by default unless another master scenario 
// configuration file is specified using a command line argument. 

// Each scenario can be run multiple times. The number of repetitions can be 
// specified using either the 'reps_all_scenarios' or 'reps_scenario_#' parameters 
// as shown below. If both parameters are specified then the 'reps_scenario_#' will 
// take precedence.

num_scenarios = 1;
reps_all_scenarios = 1;

scenario_1 = "example_scenarios/basic_two_node_network";
reps_scenario_1 = 3;
